{
  "name": "kielilah-app",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "open": "google-chrome http://localhost:3000 || chromium-browser http://localhost:3000 || xdg-open http://localhost:3000"
  },
  "engines": {
    "node": ">=14"
  }
}
